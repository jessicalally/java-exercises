public abstract class Shape {

  protected abstract double getSize();
  protected abstract void changeWidth(double w);
  protected abstract void changeHeight(double h);
  protected abstract boolean isCircle();
  protected abstract boolean isSquare();

}
